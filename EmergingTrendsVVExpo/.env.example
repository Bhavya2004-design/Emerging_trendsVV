# Outfit AI server (run: cd server && npm run dev).
# Current backend URL:
EXPO_PUBLIC_AI_API_BASE_URL=https://ladylike-elk-playmate.ngrok-free.dev

# Optional: must match server AI_SERVER_API_KEY if you set one
# EXPO_PUBLIC_AI_SERVER_API_KEY=vv-local-dev-key
